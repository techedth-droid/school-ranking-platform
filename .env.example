# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=school_ranking
# From containers use host postgres and internal port 5432
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/school_ranking

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=7d

# Backend
BACKEND_PORT=3005
NODE_ENV=development
# Comma-separated if multiple frontends (e.g. Vercel production + preview)
FRONTEND_URL=http://localhost:3007
# Production example:
# FRONTEND_URL=https://school-ranking-platform.vercel.app

# Upload
UPLOAD_PATH=/app/uploads
UPLOAD_MAX_SIZE_MB=5

# Frontend (browser calls API on host port)
NEXT_PUBLIC_API_URL=http://localhost:3005
# Production example:
# NEXT_PUBLIC_API_URL=https://school-ranking-platform-production.up.railway.app
FRONTEND_PORT=3007

# Admin seed account
ADMIN_EMAIL=admin@school.com
ADMIN_PASSWORD=admin1234
